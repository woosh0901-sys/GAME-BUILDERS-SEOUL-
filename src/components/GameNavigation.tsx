const items = [['국가 개요', 'integration-panel'], ['경제', 'economy-panel'], ['정치', 'politics-panel'], ['사회', 'society-panel'], ['외교', 'diplomacy-panel'], ['기술', 'technology-panel'], ['군사', 'military-panel'], ['정보', 'intelligence-panel'], ['역사', 'world-history-panel'], ['저장', 'save-panel']] as const
export function GameNavigation() { const move = (id: string) => document.querySelector(`.${id}`)?.scrollIntoView({ behavior: 'smooth', block: 'start' }); return <nav className="game-navigation" aria-label="주요 화면 메뉴">{items.map(([label, id]) => <button key={id} onClick={() => move(id)}>{label}</button>)}</nav> }
