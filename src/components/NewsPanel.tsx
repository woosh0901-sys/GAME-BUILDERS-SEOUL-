import { useGameStore } from '../store/gameStore'
export function NewsPanel() { const logs = useGameStore((state) => state.logs); const events = useGameStore((state) => state.eventQueue ?? []); return <section className="panel news-panel"><div className="panel-title"><h2>최근 뉴스 · 알림</h2><span className="tag">{events.length ? `중요 사건 ${events.length}` : '새 알림 없음'}</span></div><div className="news-list">{logs.slice(0, 6).map((log) => <div className={`news-item ${log.type === 'turn' ? 'important' : ''}`} key={log.id}><span>{log.type === 'turn' ? '세계' : '알림'}</span><p>{log.text}</p></div>)}</div>{events.length > 0 && <div className="event-summary"><b>결정이 필요한 사건</b><span>{events[0].title}</span></div>}</section> }
