import type { Country, RegionState, ResourceProduction } from '../../types/game'
import { addResources, emptyResources } from './resourceUtils'
import { totalResources } from '../regions/regionEngine'
export function updateResources(country: Country, regions: RegionState[]) { const production = totalResources(regions); const consumption: ResourceProduction = { oil: country.military * 0.08, iron: country.industry * 0.18, coal: country.industry * 0.12, aluminum: country.military * 0.06, rareMaterials: country.technology * 0.07, uranium: country.technology * 0.005 }; const stock = addResources(country.resourceStockpile ?? emptyResources(), production); Object.keys(stock).forEach((key) => { const typedKey = key as keyof ResourceProduction; stock[typedKey] = Math.max(0, stock[typedKey] - consumption[typedKey]) }); const shortages = Object.keys(stock).filter((key) => stock[key as keyof ResourceProduction] <= 0 && consumption[key as keyof ResourceProduction] > production[key as keyof ResourceProduction]); return { country: { ...country, resourceProduction: production.iron + production.coal + production.oil, resourceOutput: production, resourceStockpile: stock }, production, consumption, shortages } }
